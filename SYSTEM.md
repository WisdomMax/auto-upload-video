# 🧠 시스템 개요 — Momdad Fashion Diary Auto Pipeline

> **핵심 타겟 및 카테고리 설정**
> * **주요 타겟**: 60대 이상 여성 (할머니 및 어머님층) 👵
> * **핵심 카테고리**: 99% 의류 (마담 의류, 원피스, 가디건, 바지 등) 및 패션 잡화 👗
> * 에이전트는 영상을 분석하고 쿠팡 링크를 자동 스캔하거나 마케팅 원고를 창작할 때, 이 타겟에 적합한 실용성과 어조(편안함, 선물용, 고상함 등)를 최우선으로 반영합니다.

> 이 문서는 시스템의 **목적**, **작동 원리**, **전체 플로우**를 기록합니다.
> 운영 중 참고하거나, 기능 추가 시 흐름을 이해하기 위해 사용합니다.

---

## 🎯 목적

**하루 1~3개 숏폼 영상**을 최소한의 수작업으로 유튜브 쇼츠 / 틱톡 / 인스타 릴스에 동시 발행하고,
댓글을 통해 **쿠팡 파트너스 구매 전환**을 유도하는 반자동화 마케팅 파이프라인.

### 핵심 가치
- **시간 절약**: 영상 업로드 + 3사 배포를 버튼 하나로
- **검색 최적화**: 유튜브 트렌드 분석으로 검색에 유리한 제목/키워드 선택
- **수익 전환**: ManyChat DM 자동화 → 쿠팡 링크 전달 → 구매 전환
- **데이터 축적**: 어떤 제목/키워드가 효과적인지 DB에 지속 기록

---

## 🔄 전체 운영 플로우

```
1. [상품 등록]
   └── 쿠팡 링크, 상품명, 키워드, 영상 파일 입력

2. [유튜브 트렌드 분석] ← 업로드 전 반드시 실행
   └── 키워드로 YouTube Data API 검색
   └── 조회수 1,000회 이상 영상 필터링
   └── 효율 점수 계산 (조회수 ÷ 구독자수)
       ├── 🔥 그레이트 (효율 ≥ 0.1)
       ├── 👍 굿 (효율 ≥ 0.02)
       └── 📌 일반
   └── 결과를 DB에 캐싱 (동일 상품은 API 재호출 안 함)

3. [제목 & 캡션 결정]
   └── 트렌드 분석 결과 참고
   └── 그레이트/굿 영상의 제목 패턴/키워드 참고
   └── 비슷한 느낌으로 (따라하지 않고) 제목 작성
   └── AI 캡션 생성 또는 수동 작성

4. [영상 업로드 → R2]
   └── 로컬 영상 파일 → Cloudflare R2 버킷 PUT 업로드
   └── 퍼블릭 URL 발급 (CDN 제공)

5. [3사 동시 배포 → Buffer]
   └── R2 URL + 제목 + 캡션을 Buffer GraphQL API로 전송
   └── YouTube Shorts / TikTok / Instagram Reels 동시 예약/발행

6. [댓글 모니터링]
   └── 유튜브 채널 최신 댓글 자동 수집
   └── 구매 문의 댓글 발견 시
       └── 네이버 검색 유도 답변 원클릭 복사 (노출 우선)
       └── ManyChat DM으로 쿠팡 링크 자동 전달 (선택)

7. [데이터 축적]
   └── 어떤 키워드/제목이 효과적이었는지 DB 기록
   └── 다음 영상 제작 시 참고
```

---

## 🏗️ 시스템 구조

```
[브라우저 대시보드]  ← localhost:18888
       │
       │ HTTP (REST API)
       ↓
[FastAPI 백엔드 서버] (main.py)
       │
       ├── [SQLite DB] (database.py)
       │       ├── items 테이블 (상품 목록)
       │       │     └── youtube_trends (캐시된 트렌드 데이터 JSON)
       │       └── (확장 가능)
       │
       ├── [Cloudflare R2] ← 영상 파일 저장소 (CDN)
       │
       ├── [Buffer API] ← 3사 SNS 배포 허브
       │       ├── YouTube Shorts
       │       ├── TikTok
       │       └── Instagram Reels
       │
       ├── [YouTube Data API v3]
       │       ├── 트렌드 검색 (search.list)
       │       ├── 영상 상세 (videos.list)
       │       ├── 채널 정보 (channels.list)
       │       └── 댓글 수집 (commentThreads.list)
       │
       ├── [ManyChat API] ← DM 자동화
       │
       └── [Gemini AI] ← AI 캡션 생성 (선택)
```

---

## 📦 핵심 기능 상세

### 1. 유튜브 트렌드 분석 (핵심 전략 도구)

| 항목 | 내용 |
|------|------|
| 목적 | 업로드 전 경쟁 영상 분석으로 검색 유리한 제목 선택 |
| 트리거 | 상품 등록 후, 업로드 직전 1회 실행 |
| API 쿼터 | search.list(100) + videos.list(1) + channels.list(1) = 약 102 쿼터/상품 |
| 캐싱 | DB `youtube_trends` 컬럼에 저장, 재실행 시 API 미호출 |
| 필터 | 조회수 1,000회 이상만 표시 |
| 효율 공식 | `효율 = 조회수 ÷ 구독자수` |
| 라벨 | 효율 ≥ 0.1 → 🔥 그레이트 / ≥ 0.02 → 👍 굿 / 그 외 → 📌 일반 |
| 활용법 | 그레이트/굿 영상의 **키워드 패턴, 해시태그, 제목 구조** 참고 |

> ⚠️ 따라하지 말고, **"왜 이 제목이 잘 됐는지"** 패턴을 파악해서 비슷하게 응용

---

### 2. 영상 업로드 & 배포 파이프라인

```
영상 파일 선택
    │
    ▼
Cloudflare R2에 PUT 업로드
    │  (멀티파트 업로드, 대용량 지원)
    ▼
퍼블릭 CDN URL 발급
    │  예: https://pub-xxx.r2.dev/video-name.mp4
    ▼
Buffer GraphQL API 호출
    │  프로필별 (유튜브/틱톡/인스타) createPost mutation
    ▼
3사 동시 예약 또는 즉시 발행
```

**Buffer 연동 방식**: GraphQL `createPost` mutation
- 미디어 URL (R2 CDN 링크)
- 텍스트 (캡션)
- 프로필 ID (각 플랫폼 채널)
- 예약 시간 (선택)

---

### 3. 댓글 모니터링 & 구매 전환

```
유튜브 채널 댓글 자동 수집 (commentThreads.list)
    │
    ▼
대시보드에서 댓글 목록 확인
    │
    ▼
구매 문의성 댓글 발견
    │
    ├── [방법 A] 네이버 검색 유도 답변 복사
    │           └── "네이버에서 [상품명] 검색하시면 나와요! 😊"
    │               (네이버 노출 → 쿠팡 파트너스 링크 유도)
    │
    └── [방법 B] ManyChat DM 자동화
                └── 팔로우 + DM 요청 시 쿠팡 링크 자동 전송
```

---

### 4. ManyChat 자동화 (DM 전환)

| 항목 | 내용 |
|------|------|
| 목적 | 댓글로 관심 표현한 사람에게 쿠팡 링크 자동 DM 전송 |
| 트리거 | 특정 키워드 댓글 ("링크", "구매", "어디서" 등) |
| 채널 | 인스타그램 (틱톡은 API 제한으로 제외) |
| 무료 한도 | 25 Active Contacts/월 (초반 운영 충분) |
| 성장 시 | Essential 플랜 $17/월 (250명/월) |

---

## 📊 데이터 모델 (DB)

### `items` 테이블

| 컬럼 | 타입 | 설명 |
|------|------|------|
| id | INTEGER | 자동 증가 PK |
| name | TEXT | 상품명 |
| coupang_url | TEXT | 쿠팡 파트너스 링크 |
| description | TEXT | 상품 설명 |
| keywords | TEXT | 검색 키워드 (트렌드 분석에 사용) |
| video_url | TEXT | R2 업로드 후 CDN URL |
| caption | TEXT | 최종 결정된 SNS 캡션 |
| youtube_trends | TEXT | 트렌드 분석 결과 JSON (캐시) |
| created_at | DATETIME | 등록일시 |

---

## 🔑 API 쿼터 관리

| API | 무료 한도 | 예상 사용량 |
|-----|----------|------------|
| YouTube Data API | 10,000 유닛/일 | 영상당 ~102 유닛 → 약 98개 상품/일 분석 가능 |
| Buffer API | 플랜별 상이 | 발행당 1회 호출 |
| Cloudflare R2 | 10GB/월 무료 | 영상 크기에 따라 상이 |
| ManyChat | 25 컨택/월 (무료) | 초반 운영 가능 |

> 📌 하루 1~3개 영상 기준, **유튜브 API는 무료로 충분히 운영 가능**

---

## 🚀 향후 로드맵

- [ ] 쿠팡 파트너스 API 연동 (링크 자동 단축)
- [ ] 업로드 예약 스케줄러 (특정 시간 자동 발행)
- [ ] 트렌드 분석 이력 비교 (주간/월간 키워드 변화 추적)
- [ ] Gemini AI 캡션 자동 생성 연동
- [ ] 성과 추적 (조회수, 전환율 대시보드)

---

*최종 업데이트: 2026-06-07*
