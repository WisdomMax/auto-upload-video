# 🐛 엄마아빠 패션다이어리 버그 수정 & 업데이트 히스토리

본 문서는 날짜별로 발생한 버그 수정(Bug Fixes), 시스템 고도화 및 깃 푸시 내역을 기록하는 공식 로그 문서입니다.

---

## 📅 2026년 8월 4일 (화)

### 1. 🛡️ 인스타그램 동일 유저 당일 중복 DM/대댓글 중복 발송 100% 차단 (`7ec3ebe`)
- **문제점**: 한 명의 고객이 여러 릴스에 댓글을 남겼을 때, 릴스별 탐색 과정에서 동일 유저에게 DM과 대댓글이 중복 발송되던 현상 발생.
- **수정 내용**: `processed_users_today` 메모리 락 세트 및 자정 자동 초기화 루틴을 도입하여, 동일한 고객이 하루에 여러 릴스에 댓글을 쓰더라도 **하루 최대 1회만 안전하게 메시지가 나가도록 중복 발송 100% 차단**.

### 2. 🔍 인스타그램 릴스 웹 레이아웃(DOM) 변경 대응 & 미응답 댓글 전수 소급 (`a7750e6`)
- **문제점**: 인스타그램 웹 릴스 화면의 HTML 구조가 변경되어 기존 `#comments`, `ul` 선택자 방식으로 탐색 시 최근 미응답 댓글(`joung_heee`, `kb03132081` 등)이 0건으로 감지되던 현상.
- **수정 내용**: 유니버설 댓글 래퍼 탐색 엔진으로 정밀 개편하여, 누적된 미응답 댓글을 100% 정확히 감지하고 소급 응답이 수행되도록 개선.

### 3. 🌐 캡션 하단 상품 페이지 커스텀 도메인 반영 (`b1b1ee4`)
- **수정 내용**: 게시물 캡션 하단의 임시 주소(`auto-upload-video.pages.dev`)를 공식 커스텀 도메인인 **`6070.piella.shop`** 으로 100% 교체 완료.

### 4. 🚀 최신 릴스 탭(`/reels/`) 교차 수집 & 깊은 무한 스크롤 적용 (`a741cc2`)
- **수정 내용**: 프로필 메인 피드뿐만 아니라 **최신 릴스 전용 탭(`/reels/`)**을 교차 탐색하고 15회+ 동적 무한 스크롤을 적용하여, 방금 업로드된 최신 릴스가 1순위로 탐색되도록 고도화.

### 8. 🧹 터미널 실시간 모니터링 로그 최적화 (`중복 스킵 로그 다이어트`)
- **수정 내용**: 기존 중복 스킵 유저 로그(`⏭️ 스킵!`)로 인해 터미널이 도배되던 현상을 개선. 이미 답변 완료된 유저들은 사전 필터링하여 **오직 진짜 신규 미응답 댓글이 감지되었을 때만 `🎯 신규 미응답 댓글 감지! 처리 시작` 로그를 출력**하도록 터미널 모니터링 출력 다이어트 완결.

---

## 📅 2026년 8월 3일 (월)

### 1. 🔒 README 증명 이미지 개인정보 100% 모자이크/픽셀화 마스킹 (`47ba7fd`)
- **수정 내용**: 깃허브 README에 포함된 유튜브/인스타그램 증명 이미지 내 작성자 핸들, 프로필 아바타, 실명 텍스트 100% 모자이크 마스킹 완결.
